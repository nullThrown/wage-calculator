export const formatDollar = (val) => '$' + val;
export const parseDollar = (val) => val.replace(/^\$/);

// export const formatPct = (val) => val + '%';
// export const parsePct = (val) => val.replace(/^\%/);
